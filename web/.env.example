# web/.env.example
# Copy to web/.env for local `npm run dev`. Vite exposes only variables
# prefixed VITE_ to client code (anything else in your environment is
# deliberately NOT bundled, to avoid accidentally shipping server secrets
# to the browser).
VITE_API_URL=http://localhost:3000
